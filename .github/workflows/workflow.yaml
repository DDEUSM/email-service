name: Continuous Integration

on: pull_request

jobs: 
  continuous-integration: 
    runs-on: ubuntu-latest 

    steps:
      - uses: actions/checkout@v3
      - name: Run docker compose && create database && tests
        run: bash run.sh
      

